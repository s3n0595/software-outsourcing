<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cykj.mapper.QworksMapper">
    <select id="queryAllWork" resultType="java.util.Map" parameterType="int">
        select * from works t1 , demandType t2 where t1.worksTypeId = t2.demandTypeId limit #{count}, 10;
    </select>

    <select id="" resultType="int">
        SELECT count(*) from works where providerId=#{providerId};
    </select>
    <select id="" resultType="java.util.Map" parameterType="int">
        select * from providerAccount t1 , providerInfo t2 where t1.providerId = t2.providerId AND t1.providerId=#{providerId};
    </select>
</mapper>


